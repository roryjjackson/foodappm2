<div class="page">
  <div class="cards">
    <% @recipes.each_with_index do |recipe, index| %>
      <%= link_to recipe_path(recipe) do %>
        <%# <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path recipe.photo.key, height: 300, crop: :fill %>
        <div class="card-category" style="background-color: black">
          Day <%= (index / @menu.meal_type.length).round + 1 %> | <%= recipe.name %>
        </div>
      <% end %>
    <% end %>
  </div>
      <%= link_to edit_menu_path(@menu), class: "btn btn-flat" do %>
        Edit menu
      <% end %>
      <%= button_to "Destroy Menu", @menu, method: :delete, class: "btn btn-flat" %>
</div>
